[package]
name = "csv-ls"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "A lightweight language server for delimiter-separated values (CSV/TSV/SSV/PSV)."
publish = false

[dependencies]
lsp-server = "0.8"
lsp-types = "0.97"
serde_json = "1"
